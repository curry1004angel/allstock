# 컨텍스트 노트: 4Q 재생성 보정 (2026-07-11)

## 문제 확정 과정에서의 발견

- 처음 보고된 "연간 있고 1~3Q < 3개" 934건 중, **1~3Q가 전부 없는 그룹(7,617 계정건)은
  이미 안전**했다. generate_q4의 `how="inner"` 조인이 분기 데이터가 하나도 없는
  (종목,연도,계정)을 걸러내 4Q를 만들지 않기 때문. 실제 오염은 **일부만 있는** 경우:
  계정 단위 2,801건 = (종목,연도) 1,123건, 종목 922개, 2025년 이후 100건.
- 오염 케이스의 80%(2,243건)는 3Q 보고서가 존재 → DART `thstrm_add_amount`
  (당기누적금액, 연초~해당분기)로 1~3Q 합을 공시값 그대로 복원 가능.
  API 문서로 필드 존재 확인, 삼성전자 2024 분기값으로 기존 `thstrm_amount`가
  3개월 단일값임을 검증함 (1Q 71.9조 → 2Q 74.1조, 누적이었다면 ~146조).

## 설계 결정과 이유

1. **복원 우선순위: 1~3Q 완전하면 합(기존 방식) > 3Q 누적 > NaN.**
   누적은 불완전 연도에만 쓴다 — 현재 정상인 4Q 값이 바뀌는 회귀 위험 제로.
   합과 누적이 둘 다 있는데 어긋나는 건수는 실행 시 경고로 출력(공시 정정 감지).
2. **복원 불가 시 행 삭제가 아니라 금액 NaN 행 유지.**
   process_quarterly와 웹앱 load_fundamentals 모두 위치 기반 shift(1)로 전년/전분기
   비교를 하므로, 행을 빼면 다음 해 4Q YoY가 2년 전과 비교되는 새 왜곡 발생(193건 해당).
   NaN 행이면 YoY→NaN, 웹앱 status→"데이터부족", streak 중단으로 안전 강하.
   (분기 전무 그룹은 기존대로 행 없음 유지 — 보고된 문제 범위 밖이라 손대지 않음.)
3. **amount 컬럼 dtype이 int64 → float64로 바뀜.** NaN 도입의 불가피한 결과.
   웹앱은 나눗셈/비교만 하므로 영향 없음(표시도 이미 나눗셈 후 float).
   단, fetch_cashflow.py(WIP)의 `int(r.amount)` 딕셔너리 빌드가 NaN에서 크래시
   → notna 필터 추가로 선제 수정.
4. **cum_amount는 quarterly.parquet의 컬럼으로 저장, annual에는 저장 안 함.**
   3Q 누적만 실제로 쓰이지만 파싱 비용이 없어 전 분기 저장.
5. **fetch_cashflow에는 누적 저장 안 함.** CF 분기 행은 차분 성공(=이전 분기 확보)
   시에만 생기므로 그 행의 누적은 항상 "완전" 케이스 → 복원에 기여 없음.
   CF 불완전 연도는 새 generate_q4 규칙에 따라 자동으로 NaN 처리됨.

## 남은 운영 절차

- 코드 push 후 Actions "과거 데이터 백필" 워크플로를 **재무만(skip_prices)**,
  2016~2024로 1회 실행하면 cum_amount가 채워져 80%가 복원됨.
  2025~2026은 매주 토요일 재무 수집이 전체 재수집하므로 자동 반영.
- 백필 전까지는 오염 4Q가 전부 NaN(빈 값) 상태 — 틀린 값보다 낫다는 사용자 결정.
- 2015년은 분기 데이터가 통째로 없는 그룹이라 백필해도 복원 대상 아님.

---

# 업종 분류 백필 + 애널리스트 컨센서스 수집 (2026-07-30)

analysis-cards 카드에 업종 내 백분위와 컨센서스 섹션을 넣기 위한 데이터 확보.
요청 출처는 stock-analysis-app — 카드를 환각 탐지 대조 기준으로 쓰는데 피어 비교·
밸류에이션 항목이 카드에 없어 대조가 원천적으로 불가능했다.

## 결정 사항

1. **업종 분류 출처는 yfinance per-ticker info.**
   FinanceDataReader의 StockListing('KRX')에는 업종이 없다(17컬럼 확인). 야후는
   한국 종목에도 sector/industry를 준다. 2765종목 1회 백필로 2281종목(82.5%) 확보.
   미확보 484종목은 리츠·일부 우선주·스팩 등 야후 커버리지 자체가 없는 종목이다.
2. **stock_list.csv에 sector·industry 두 컬럼 추가.** 컬럼 추가라 모든 행이 diff로
   잡히지만(2766줄) 불가피하다. 이름 기반으로 읽는 소비자에겐 하위호환이다.
3. **개행을 LF로 고정.** 형제 스크립트들은 lineterminator를 지정하지 않아 윈도우에서
   돌리면 CRLF가 된다. CI(리눅스)가 쓴 기존 파일과 전 줄이 어긋나고 실행 환경마다
   개행이 뒤집히므로 이 스크립트만 LF를 명시했다.
4. **백필은 재개 가능하게.** 이미 industry가 있는 행은 건너뛰고 25종목마다 임시 파일에
   써서 교체한다. 2765종목이 30분 넘게 걸려 중단 위험이 실재한다.
5. **컨센서스 수집은 별도 스크립트(fetch_analyst.py).** 카드 생성기는 네트워크를 타지
   않는다 — 같은 종목 카드가 실행 시각마다 달라지면 리포트에 박히는 card_sha256이
   매번 바뀌어 재현성이 깨진다. 수집은 여기서만 하고 asof 날짜를 붙여 캐시한다.
6. **값 없음은 None으로 저장.** 0으로 채우면 소비 측이 실제 값으로 읽어 거짓 불일치를
   낸다. 소형주(211050 등)는 컨센서스·PER이 전부 없는 것이 정상 경로다.
7. **한국 PER·PBR은 야후에서 못 받는다.** fundamentals 엔드포인트가 404다
   (`No fundamentals data found for symbol: 005930.KS`). trailingPE·priceToBook이
   None으로 온다. 카드 쪽에서 시가총액 ÷ 순이익, 시가총액 ÷ 자본총계로 직접 계산해
   `PER(계산)`·`PBR(계산)` 행으로 병기하기로 했다. forwardPE·EV/EBITDA는 다른 경로라 나온다.

## 남은 운영 절차

- 업종 미확보 484종목은 재실행해도 대부분 그대로다(야후에 없음). 새 상장분만 채워진다.
- analyst 캐시는 현재 005930·211050 2종목뿐이다. 전 종목은
  `python scripts/fetch_analyst.py --all`이며 2765종목 × info 호출이라 장시간 작업이다.
- 컨센서스는 스냅샷이라 주기적 재수집이 필요하다. asof가 카드에 찍히므로 낡으면 드러난다.
